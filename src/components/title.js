import React from 'react';

const Titles = () =>(
    <div>
        <h1>Weather-Finder</h1>
        <p>Find out Temperature, Weather conditiond and many more.</p>
    </div>
);

export default Titles;